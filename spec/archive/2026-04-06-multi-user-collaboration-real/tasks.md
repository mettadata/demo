# Tasks for multi-user-collaboration-real

## Batch 1 (no dependencies)

- [ ] **Task 1.1: Create broadcastSync.ts module**
  - **Files**: `src/lib/sync/broadcastSync.ts` (create new; also create `src/lib/sync/` directory)
  - **Action**: Implement the BroadcastChannel wrapper module. Create a module-level `BroadcastChannel` singleton on channel name `"metta-todo-sync"`, guarded by `typeof window === 'undefined'` so SSR renders a no-op module. Export `SyncMessage` discriminated union (`"todos-updated"`, `"kanban-updated"`, `"presence-heartbeat"`) and `HeartbeatPayload` interface. Export `broadcastTodos(todos: Todo[]): void`, `broadcastKanban(state: KanbanState): void`, and `broadcastHeartbeat(c: { id: string; name: string; color: string }): void` — each posts the corresponding typed message with `channel.postMessage(...)`. Export `listenForRemoteUpdates(onTodos, onKanban, onHeartbeat): () => void` which attaches a single `"message"` event listener that routes by `type`, silently ignores unknown types, wraps the handler body in a try/catch to swallow parse errors, and returns a cleanup function that calls `removeEventListener`. If `BroadcastChannel` construction fails (pre-Safari 15.4), all four exported functions must be no-ops and `listenForRemoteUpdates` must return `() => {}`. Import `Todo` from `../stores/todos.js` and `KanbanState` from `../stores/kanban.js` as type-only imports to avoid circular runtime dependencies.
  - **Verify**: Run `npx tsc --noEmit` from the `demo/` directory with no errors. Manually confirm the module exports compile correctly. Confirm SSR guard: instantiating in a Node context (no `window`) must not throw.
  - **Done**: `src/lib/sync/broadcastSync.ts` exists; all four functions are exported with the correct signatures matching the API contract in `contracts/broadcast-sync-api.md`; the file compiles without TypeScript errors; BroadcastChannel construction is guarded for SSR; all functions degrade to no-ops when the channel is unavailable.

- [ ] **Task 1.2: Create collaborators.ts store**
  - **Files**: `src/lib/stores/collaborators.ts` (create new)
  - **Action**: Implement the collaborator identity and presence store. Export the `Collaborator` interface (`id`, `name`, `color`, `lastSeen: number`). Implement `deriveColor(id: string): string` using the multiplicative 32-bit hash (`hash = (hash * 31 + charCodeAt(i)) >>> 0`) modulo the 12-color `AVATAR_PALETTE` from the data model schema. Implement `getInitials(name: string): string` using the avatar initials algorithm: split on whitespace; two-or-more words → first char of first + first char of last, uppercased; one word → first two chars uppercased; empty → `"??"`. Initialize the `self` writable store synchronously from `localStorage` inside a `typeof window !== 'undefined'` guard: read or generate `"user-id"` with `crypto.randomUUID()`; read `"user-name"` defaulting to `"Anonymous"` without writing to `localStorage` yet (first-visit prompt handles the write). Export `activeCollaborators: Writable<Collaborator[]>` initialized to `[]`. Export `updateSelfName(name: string): void` which trims, rejects empty strings, writes to `localStorage["user-name"]`, and calls `self.update(...)`. Export `destroyPresence(): void` as a stub (interval wiring is added in Batch 2). All `localStorage` access must be guarded for SSR. Do NOT import from `broadcastSync.ts` yet — that wiring is added in Batch 2.
  - **Verify**: Run `npx tsc --noEmit`. Confirm `deriveColor` returns the same hex string for the same `id` on two calls. Confirm `getInitials("Grace Hopper")` returns `"GH"`, `getInitials("Anonymous")` returns `"AN"`, `getInitials("Bob")` returns `"BO"`, `getInitials("")` returns `"??"`.
  - **Done**: `src/lib/stores/collaborators.ts` exists; `Collaborator` interface, `self`, `activeCollaborators`, `updateSelfName`, `destroyPresence`, `deriveColor`, and `getInitials` are all exported; `self.id` is populated from `localStorage` or a new UUID on first import; `self.color` is deterministic; file compiles without TypeScript errors.

- [ ] **Task 1.3: Add actorId param to all mutation functions in todos.ts**
  - **Files**: `src/lib/stores/todos.ts` (modify)
  - **Action**: Add an optional `actorId?: string` trailing parameter to all 15 mutation functions: `addTodo`, `toggleTodo`, `removeTodo`, `updateTodo`, `archiveTodo`, `unarchiveTodo`, `addComment`, `editComment`, `deleteComment`, `addAttachment`, `removeAttachment`, `addReply`, `editReply`, `deleteReply`, and `moveCard` (note: `moveCard` is currently in `kanban.ts` — only the 14 functions that are actually in `todos.ts` get the parameter here; `moveCard` is handled via kanban.ts in Batch 2). For functions that append an `ActivityEvent` (`addTodo` → `created`, `toggleTodo` → `completed`/`uncompleted`, `updateTodo` → `edited`, `addAttachment` → `attachment_added`, `removeAttachment` → `attachment_removed`, `archiveTodo` → `archived`, `unarchiveTodo` → `unarchived`): when `actorId` is provided, spread `{ actorId }` into the event's `detail` field alongside any pre-existing detail fields. For `addTodo`: the `created` event has no prior `detail`, so `detail` becomes `{ actorId }` when provided. For `updateTodo`: the existing `edited` events carry `{ field, from, to }` — add `actorId` alongside those when provided. For functions that do not append an `ActivityEvent` (`removeTodo`, `editComment`, `deleteComment`, `addReply`, `editReply`, `deleteReply`): accept but do not embed `actorId` in any persistent field. Do NOT add any `broadcastTodos` calls yet — that is Batch 2. Ensure all existing callers continue to compile with no argument changes required.
  - **Verify**: Run `npx tsc --noEmit`. Confirm no existing call sites need to be updated. Confirm `addTodo("test", "user-1")` would produce a `created` event with `detail: { actorId: "user-1" }`. Confirm `addTodo("test")` produces a `created` event with no `detail` key.
  - **Done**: All 14 mutation functions in `todos.ts` accept `actorId?: string`; `ActivityEvent`-appending functions include `{ actorId }` in `detail` when it is provided; existing callers with no `actorId` argument continue to work; file compiles without TypeScript errors.

---

## Batch 2 (depends on Batch 1)

- [ ] **Task 2.1: Wire broadcast calls into todos.ts mutations**
  - **Depends on**: Task 1.1, Task 1.3
  - **Files**: `src/lib/stores/todos.ts` (modify)
  - **Action**: Import `broadcastTodos` from `../sync/broadcastSync.js` at the top of `todos.ts`. At the end of each mutation function body — after the `todos.update(...)` or `todos.set(...)` call that commits the change — add `broadcastTodos(get(todos))`. Import `get` from `svelte/store` (it is not currently imported; add it). Apply this to: `addTodo`, `toggleTodo`, `removeTodo`, `updateTodo`, `archiveTodo`, `unarchiveTodo`, `addComment`, `editComment`, `deleteComment`, `removeAttachment`, `addReply`, `editReply`, `deleteReply`. For `addAttachment` specifically: the broadcast call must be placed only on the success path, after confirming `getLastPersistError()` returns `null` and before returning `true`. On the revert path (quota exceeded, `todos.set(previousTodos)` called), do NOT broadcast — this ensures remote tabs do not receive a quota-failed attachment. The broadcast must NOT be placed inside the `todos.update` callback itself, and must NOT be added to the `todos.subscribe` persistence handler.
  - **Verify**: Run `npx tsc --noEmit`. Open two browser tabs on `localhost:5173`. Add a todo in tab A; tab B must display it within 200 ms without reload. Delete a todo in tab A; tab B must reflect the deletion. Toggle a todo in tab A; tab B must show the updated completed state.
  - **Done**: All 14 mutation functions in `todos.ts` call `broadcastTodos(get(todos))` after their store write; `addAttachment` only broadcasts on the success path; `broadcastTodos` is imported from `broadcastSync.ts`; `get` is imported from `svelte/store`; file compiles without TypeScript errors.

- [ ] **Task 2.2: Wire broadcast calls into kanban.ts mutations**
  - **Depends on**: Task 1.1
  - **Files**: `src/lib/stores/kanban.ts` (modify)
  - **Action**: Import `broadcastKanban` and `broadcastTodos` from `../sync/broadcastSync.js`. Import `get` from `svelte/store` (already imported). After each kanban store write in the following functions, add the indicated broadcast calls: `addColumn` → `broadcastKanban(get(kanbanState))`; `renameColumn` → `broadcastKanban(get(kanbanState))`; `deleteColumn` → `broadcastKanban(get(kanbanState))`; `moveColumn` → `broadcastKanban(get(kanbanState))`; `applyTemplate` → `broadcastKanban(get(kanbanState))` AND `broadcastTodos(get(todos))` (both stores are replaced wholesale); `moveCard` → `broadcastKanban(get(kanbanState))` after the `kanbanState.update(...)` call AND `broadcastTodos(get(todos))` after the `todos.update(...)` call that appends the `moved` event. For `moveCard`, also add the `actorId?: string` optional parameter, pass it to the `todos.update` block where the `moved` `ActivityEvent` is constructed so that `detail` includes `{ fromColumn, toColumn, actorId }` when `actorId` is provided. Broadcast calls must be placed after the store update in the function body, not inside the updater callback.
  - **Verify**: Run `npx tsc --noEmit`. Open two tabs. Rename a column in tab A — tab B must show the new title within 200 ms. Add a column in tab A — tab B must show the new column. Move a card in tab A — tab B must show the card in the new column and the `moved` event in the activity log with `actorId` present when supplied.
  - **Done**: `addColumn`, `renameColumn`, `deleteColumn`, `moveColumn`, `applyTemplate`, and `moveCard` all call `broadcastKanban` after their kanban store write; `applyTemplate` and `moveCard` additionally call `broadcastTodos`; `moveCard` accepts and threads `actorId` into the `moved` event `detail`; file compiles without TypeScript errors.

- [ ] **Task 2.3: Wire presence heartbeat into collaborators.ts**
  - **Depends on**: Task 1.1, Task 1.2
  - **Files**: `src/lib/stores/collaborators.ts` (modify)
  - **Action**: Import `broadcastHeartbeat` and `listenForRemoteUpdates` from `../sync/broadcastSync.js`. Inside a `typeof window !== 'undefined'` block, implement the full presence lifecycle: (1) Send initial heartbeat on module load by calling `broadcastHeartbeat({ id: $self.id, name: $self.name, color: $self.color })` using `get(self)` to read current values. Update `self` to set `lastSeen = Date.now()` before sending. (2) Start a heartbeat interval with `setInterval` at 30 000 ms that re-sends the heartbeat and updates `self.lastSeen`. Store the interval handle. (3) Start an expiry poll interval with `setInterval` at 10 000 ms that calls `activeCollaborators.update(list => list.filter(c => Date.now() - c.lastSeen <= 90_000))`. Store the interval handle. (4) Call `listenForRemoteUpdates` with a no-op `onTodos` (`() => {}`), a no-op `onKanban` (`() => {}`), and an `onHeartbeat` handler that: checks `payload.id !== get(self).id` before upserting; upserts by finding an existing entry with matching `id` and replacing it, or prepending a new entry, setting `lastSeen` from the payload. Store the unsubscribe callback. (5) Implement `destroyPresence()` to clear both interval handles, call the unsubscribe callback, and close the channel via the handle returned from the `listenForRemoteUpdates` call (or accept that channel close is managed in `broadcastSync.ts` — align with the design: `destroyPresence` calls `clearInterval` on both timers and calls the unsubscribe function; channel lifetime is owned by `broadcastSync.ts`).
  - **Verify**: Run `npx tsc --noEmit`. Open two tabs. Within 200 ms of opening tab B, tab A's `activeCollaborators` store should contain tab B's entry (visible via Svelte DevTools or a temporary `console.log` in `onMount`). Wait 91 seconds with tab B closed; tab A's `activeCollaborators` should drop that entry on the next 10-second poll.
  - **Done**: `collaborators.ts` sends a heartbeat on load and every 30 s; active collaborators are upserted on receipt; stale entries (>90 s) are evicted every 10 s; `destroyPresence` stops both intervals and unregisters the listener; self is never added to `activeCollaborators`; file compiles without TypeScript errors.

---

## Batch 3 (depends on Batch 2)

- [ ] **Task 3.1: Add remote listener setup in +page.svelte**
  - **Depends on**: Task 2.1, Task 2.2, Task 2.3
  - **Files**: `src/routes/+page.svelte` (modify)
  - **Action**: Import `listenForRemoteUpdates` from `$lib/sync/broadcastSync.js`. Import `todos` from `$lib/stores/todos.js` and `kanbanState` from `$lib/stores/kanban.js` (store references for remote writes). Import `destroyPresence` from `$lib/stores/collaborators.js`. Inside the existing `onMount` callback, alongside the existing `handleKeydown` listener registration, call `listenForRemoteUpdates` with: `onTodos: (t) => todos.set(t)`, `onKanban: (s) => kanbanState.set(s)`, `onHeartbeat: () => {}` (heartbeat is handled inside `collaborators.ts`). Store the returned unsubscribe function. In the `onMount` cleanup return function, call `unsubscribe()` and `destroyPresence()` in addition to the existing `removeEventListener` call. The `todos.set(t)` and `kanbanState.set(s)` calls must be direct store writes, NOT going through any mutation function, to preserve history isolation by construction.
  - **Verify**: Run `npx tsc --noEmit`. Open two tabs. Verify that a todo added in tab A appears in tab B and does NOT create an extra undo entry in tab B (check `canUndo` is `false` in tab B after receiving a remote mutation if tab B has performed no local mutations). Verify `destroyPresence` is called on hot-module replacement by adding a temporary `console.log` inside it.
  - **Done**: `listenForRemoteUpdates` is called in `onMount`; cleanup calls `unsubscribe()` and `destroyPresence()`; remote `todos` and `kanbanState` writes go through `store.set()` not mutation functions; history isolation is preserved; file compiles without TypeScript errors.

- [ ] **Task 3.2: Add first-visit name prompt banner in +page.svelte**
  - **Depends on**: Task 2.3
  - **Files**: `src/routes/+page.svelte` (modify)
  - **Action**: Import `self` and `updateSelfName` from `$lib/stores/collaborators.js`. Add local reactive state: `let showNamePrompt = $state(false)` and `let nameInput = $state('')`. In `onMount`, after the existing setup, check `localStorage.getItem('user-name') === null` and if so set `showNamePrompt = true`. In the template, add a dismissible fixed-bottom banner (conditionally rendered when `showNamePrompt` is `true`): a `<div>` with `role="dialog"`, `aria-label="Set your display name"`, and `aria-modal="false"`. Inside the banner: a short prompt text, a text `<input>` bound to `nameInput` with `aria-label="Display name"`, a confirm `<button>` that calls a `confirmName()` handler, and a dismiss `<button>` that calls a `dismissPrompt()` handler. Implement `confirmName()`: trim `nameInput`; if empty, do nothing (prompt stays open); otherwise call `updateSelfName(trimmed)` and set `showNamePrompt = false`. Implement `dismissPrompt()`: call `updateSelfName('Anonymous')` (which writes `"Anonymous"` to `localStorage["user-name"]`, preventing re-prompt on next load), then set `showNamePrompt = false`. Style the banner with Tailwind: `fixed bottom-0 left-0 right-0 bg-white dark:bg-gray-800 border-t border-gray-200 dark:border-gray-700 p-4 flex items-center gap-3 shadow-lg z-50`.
  - **Verify**: Clear `localStorage` and reload. Banner must appear. Type a name and confirm — banner closes, `localStorage.getItem("user-name")` equals the entered name, `$self.name` equals the entered name. Reload — banner must NOT appear. Clear `localStorage`, reload, dismiss without typing — banner closes, `localStorage.getItem("user-name")` equals `"Anonymous"`. Reload again — banner must NOT appear.
  - **Done**: First-visit banner appears when `"user-name"` is absent from `localStorage`; confirming a non-empty name persists it and closes the banner; dismissing writes `"Anonymous"` and closes the banner; empty submission is rejected; banner does not appear on subsequent loads; file compiles without TypeScript errors.

- [ ] **Task 3.3: Add editable name control in page header**
  - **Depends on**: Task 2.3
  - **Files**: `src/routes/+page.svelte` (modify)
  - **Action**: (Imports from Task 3.2 already cover what is needed.) Add local reactive state: `let showNameEditor = $state(false)` and `let nameEditorInput = $state('')`. In the header `<div class="flex items-center gap-2">`, add a name-edit button immediately before `<UndoRedoButtons />`. The button should display a pencil SVG icon (use an inline `<svg>` matching the style of the existing keyboard shortcuts button: `p-2 rounded-lg text-gray-600 hover:bg-gray-200 dark:text-gray-300 dark:hover:bg-gray-700 transition-colors`) with `aria-label="Edit your display name"`. When clicked, set `nameEditorInput = $self.name` and `showNameEditor = true`. Render the inline editor as a small `<div>` appearing immediately after the button (or as an absolutely positioned popover anchored to the button) that is conditionally shown when `showNameEditor` is `true`. The editor contains: a text `<input>` bound to `nameEditorInput` with `aria-label="Your display name"` pre-filled from `$self.name`, a save `<button>`, and a cancel `<button>`. Implement save: trim `nameEditorInput`; if empty, do nothing; otherwise call `updateSelfName(trimmed)` and set `showNameEditor = false`. Implement cancel: set `showNameEditor = false`. The control must be keyboard-navigable (Tab-reachable, Enter-submittable).
  - **Verify**: Run `npx tsc --noEmit`. Confirm the pencil button is in the page header DOM. Tab to it and press Enter — editor opens pre-filled with current name. Change the name and submit — `$self.name` updates immediately and `localStorage.getItem("user-name")` equals the new name. Submit empty input — editor remains open, name unchanged. Press cancel — editor closes, name unchanged.
  - **Done**: Pencil-icon button is always visible in the page header; activating it opens an inline editor pre-filled with `$self.name`; valid name submission updates `self` and `localStorage`; empty submission is rejected; cancel closes without side effects; control is keyboard-accessible; file compiles without TypeScript errors.

---

## Batch 4 (depends on Batch 3)

- [ ] **Task 4.1: Add last-actor avatar to KanbanCard.svelte**
  - **Depends on**: Task 3.1, Task 3.2, Task 3.3
  - **Files**: `src/lib/components/KanbanCard.svelte` (modify)
  - **Action**: Import `self` and `activeCollaborators` from `$lib/stores/collaborators.js`. Import `getInitials`, `deriveColor` are internal to `collaborators.ts` — instead expose a helper or compute inline. Since `getInitials` and `deriveColor` are not currently exported from `collaborators.ts`, export them from `collaborators.ts` now (add to the exports in Task 1.2 / amend the file). Import `getInitials` from `$lib/stores/collaborators.js`. Add a `$derived` variable `lastActorAvatar` that: iterates `todo.activityLog` to find the entry with the maximum `timestamp` where `event.detail?.actorId` is a non-empty string; if found, looks up the `actorId` first in `$activeCollaborators` (by `id`), then checks `$self.id`; if a match is found, builds `{ initials: getInitials(collaborator.name), color: collaborator.color, name: collaborator.name }`; if no match, resolves to `null`. In the card's top flex row (`<div class="flex items-center gap-2">`), add a conditional avatar element at the end of the row:
    ```svelte
    {#if lastActorAvatar}
      <span
        class="w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold text-white flex-shrink-0"
        style="background-color: {lastActorAvatar.color}"
        aria-label={lastActorAvatar.name}
        title={lastActorAvatar.name}
      >{lastActorAvatar.initials}</span>
    {/if}
    ```
  - **Verify**: Run `npx tsc --noEmit`. Move a card from one column to another (providing `actorId = get(self).id` in a manual browser console call or by wiring it via the UI in Task 3). The card should show a small circular avatar with the user's initials. A card with only a `created` event and no `actorId` in its detail must show no avatar.
  - **Done**: `KanbanCard.svelte` renders a circular avatar for the last actor when `detail.actorId` is present and matches a known collaborator or self; no avatar is rendered when no matching actorId is found; avatar has `aria-label` with the collaborator's name; initials algorithm is consistent with `collaborator-data-model.md`; file compiles without TypeScript errors.

- [ ] **Task 4.2: Add "who is here" pill row to KanbanBoard.svelte**
  - **Depends on**: Task 3.1, Task 3.2, Task 3.3
  - **Files**: `src/lib/components/KanbanBoard.svelte` (modify)
  - **Action**: Import `self` and `activeCollaborators` from `$lib/stores/collaborators.js`. Import `getInitials` from `$lib/stores/collaborators.js`. Above the existing `<div class="flex gap-4 overflow-x-auto pb-4 min-h-[300px]">` board container, add a "who is here" section:
    ```svelte
    <div class="flex items-center gap-2 mb-2" aria-label="Active collaborators">
      <span class="text-xs text-gray-500 dark:text-gray-400 mr-1">Here:</span>
      <!-- Self avatar with ring to distinguish from remotes -->
      <span
        class="w-7 h-7 rounded-full flex items-center justify-center text-xs font-bold text-white ring-2 ring-white dark:ring-gray-900 flex-shrink-0"
        style="background-color: {$self.color}"
        aria-label={$self.name}
        title="{$self.name} (you)"
      >{getInitials($self.name)}</span>
      <!-- Remote collaborator avatars -->
      {#each $activeCollaborators as collaborator (collaborator.id)}
        <span
          class="w-7 h-7 rounded-full flex items-center justify-center text-xs font-bold text-white flex-shrink-0"
          style="background-color: {collaborator.color}"
          aria-label={collaborator.name}
          title={collaborator.name}
        >{getInitials(collaborator.name)}</span>
      {/each}
    </div>
    ```
    The self avatar must always be present. When `$activeCollaborators` is empty, only the self avatar appears. The self avatar is visually distinguished by the `ring-2 ring-white dark:ring-gray-900` classes.
  - **Verify**: Run `npx tsc --noEmit`. Open one tab — only one avatar (self) appears in the "who is here" row. Open a second tab — both tabs should each show two avatars within 200 ms. Close the second tab and wait 91 seconds — the first tab's "who is here" row should revert to one avatar. Each avatar has an `aria-label` attribute containing the collaborator's name.
  - **Done**: `KanbanBoard.svelte` renders a "who is here" row with one self avatar (ringed) plus one avatar per `activeCollaborators` entry; total count equals `activeCollaborators.length + 1`; each avatar has `aria-label`; self avatar is visually distinguished; row updates reactively when `activeCollaborators` changes; file compiles without TypeScript errors.

---

## Batch 5 (depends on Batch 4)

- [ ] **Task 5.1: Write tests for broadcastSync.ts and collaborators.ts**
  - **Depends on**: Task 4.1, Task 4.2 (all source complete)
  - **Files**: `src/lib/sync/broadcastSync.test.ts` (create new), `src/lib/stores/collaborators.test.ts` (create new)
  - **Action**: Write Vitest tests in `src/lib/sync/broadcastSync.test.ts`. All tests run in a `jsdom` environment. Mock `BroadcastChannel` as a class that records `postMessage` calls and allows manually dispatching `MessageEvent`s. Test cases: (1) `broadcastTodos` posts a message with `type: "todos-updated"` and the correct payload; (2) `broadcastKanban` posts a message with `type: "kanban-updated"`; (3) `broadcastHeartbeat` posts a message with `type: "presence-heartbeat"` and a `lastSeen` close to `Date.now()`; (4) `listenForRemoteUpdates` calls `onTodos` when a `"todos-updated"` message arrives; (5) `listenForRemoteUpdates` calls `onKanban` when a `"kanban-updated"` message arrives; (6) `listenForRemoteUpdates` calls `onHeartbeat` when a `"presence-heartbeat"` message arrives; (7) unknown message types are silently ignored — neither callback is called and no error is thrown; (8) the unsubscribe callback returned by `listenForRemoteUpdates` stops future delivery. Write Vitest tests in `src/lib/stores/collaborators.test.ts`. Test cases: (1) first load with no `"user-id"` in `localStorage` generates a UUID and writes it under `"user-id"`; (2) existing `"user-id"` is reused without generating a new UUID; (3) `self.color` is the same value on two separate initializations of the same `id` (deterministic); (4) `self.name` defaults to `"Anonymous"` when `"user-name"` is absent; (5) `updateSelfName("Alice")` sets `localStorage["user-name"]` to `"Alice"` and updates `get(self).name`; (6) `updateSelfName("  ")` (whitespace-only) does nothing; (7) `getInitials("Grace Hopper")` returns `"GH"`; (8) `getInitials("Anonymous")` returns `"AN"`; (9) `getInitials("")` returns `"??"`. Use `beforeEach` to reset `localStorage` via a mock or by using `vitest`'s `localStorage` stub. Isolate each test to avoid state leakage between tests.
  - **Verify**: Run `npx vitest run src/lib/sync/broadcastSync.test.ts src/lib/stores/collaborators.test.ts` — all tests pass with no failures.
  - **Done**: Both test files exist; all described test cases are implemented; `npx vitest run` exits with code 0 for both files; no test is skipped or pending.

- [ ] **Task 5.2: Write tests for updated todos.ts mutations (actorId + broadcast)**
  - **Depends on**: Task 4.1, Task 4.2
  - **Files**: `src/lib/stores/todos.test.ts` (create new or extend if it exists)
  - **Action**: Check if `src/lib/stores/todos.test.ts` already exists; if so, extend it rather than replacing it. Write test cases covering the new behavior introduced in this change. Mock `broadcastTodos` from `../sync/broadcastSync.js` using `vi.mock` so tests do not depend on a real `BroadcastChannel`. Test cases: (1) `addTodo("Buy milk", "user-1")` creates a todo whose `activityLog[0]` has `type: "created"` and `detail.actorId === "user-1"`; (2) `addTodo("Buy milk")` (no actorId) creates a todo whose `activityLog[0]` has `type: "created"` and no `actorId` key in `detail`; (3) `toggleTodo(id, "user-2")` on an incomplete todo appends an event with `type: "completed"` and `detail.actorId === "user-2"`; (4) `archiveTodo(id, "user-4")` appends an event with `type: "archived"` and `detail.actorId === "user-4"`; (5) `broadcastTodos` is called once per mutation invocation (verify the mock was called once after `addTodo`); (6) `addAttachment` calls `broadcastTodos` on success and does NOT call `broadcastTodos` when quota is exceeded (simulate quota error by making `localStorage.setItem` throw, then verify mock call count); (7) remote `todos.set(payload)` does NOT increment `undoStack` length (import `canUndo` from `history.ts` and confirm it remains `false` after a direct `todos.set` call). Use `beforeEach` to reset the `todos` store to `[]` and clear `localStorage`.
  - **Verify**: Run `npx vitest run src/lib/stores/todos.test.ts` — all tests pass.
  - **Done**: All described test cases are implemented; `broadcastTodos` mock is verified per mutation; `actorId` embedding in `detail` is tested for both present and absent cases; undo stack isolation from remote writes is tested; `npx vitest run` exits with code 0 for the file.

- [ ] **Task 5.3: Write component tests for KanbanCard avatar and KanbanBoard presence row**
  - **Depends on**: Task 4.1, Task 4.2
  - **Files**: `src/lib/components/KanbanCard.test.ts` (create new), `src/lib/components/KanbanBoard.test.ts` (create new or extend if exists)
  - **Action**: Write Vitest + `@testing-library/svelte` component tests. For `KanbanCard.test.ts`: (1) render a `KanbanCard` with a `todo` whose `activityLog` contains a `"moved"` event with `detail.actorId: "user-1"`, and `activeCollaborators` containing `{ id: "user-1", name: "Eve", color: "#aabbcc", lastSeen: Date.now() }` — assert that a DOM element with `aria-label="Eve"` is present and `style` contains `background-color: #aabbcc`; (2) render with `actorId` matching `self.id` and `self.name = "Frank"` — assert an avatar with `aria-label="Frank"` is present; (3) render with a `todo` whose `activityLog` has no `detail.actorId` — assert no element with a `rounded-full` class that is 24px × 24px exists in the card (or that no `aria-label` for a collaborator name is present); (4) render with `name: "Grace Hopper"` — assert the avatar text content is `"GH"`. For `KanbanBoard.test.ts`: (1) render `KanbanBoard` with empty `activeCollaborators` — assert exactly one avatar element in the "who is here" section with `aria-label` matching `self.name`; (2) render with two entries in `activeCollaborators` (`"Alice"` and `"Bob"`) — assert three avatars are present; (3) assert the self avatar has the `ring-2` class that remote avatars do not. Mock the `kanbanBoard` store and `isBoardPristine` to return a minimal value so the full board does not need to render. Mock `collaborators.ts` stores directly by overriding the writable stores.
  - **Verify**: Run `npx vitest run src/lib/components/KanbanCard.test.ts src/lib/components/KanbanBoard.test.ts` — all tests pass.
  - **Done**: Both test files exist; avatar rendering with a matching actorId, self-actorId, and no actorId are all tested; initials derivation is spot-checked; "who is here" count and self-ring distinction are tested; `npx vitest run` exits with code 0 for both files.
