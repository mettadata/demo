# Tasks for notification-system-alerts-due

## Batch 1 — New files with no dependencies on each other or existing modified files

- [ ] **Task 1.1: Create notifications store**
  - **Files**: `src/lib/stores/notifications.ts` (new)
  - **Action**: Create the module. Export the `Notification` interface and `NotificationType` union type. Export a `writable<Notification[]>` store named `notifications`. Export `push(notification: Omit<Notification, 'id' | 'createdAt'>, _fromChannel?: boolean): string` — generates `id` via `crypto.randomUUID()` and `createdAt` via `new Date().toISOString()`, evicts `queue[0]` when `queue.length >= 5` before appending, posts a `notification-pushed` message to the `metta-todo-sync` BroadcastChannel when `_fromChannel` is falsy. Export `dismiss(id: string, _fromChannel?: boolean): void` — filters by id, posts `notification-dismissed` when `_fromChannel` is falsy. Export `clearAll(): void` — sets store to `[]`, no broadcast. Register a BroadcastChannel listener at module init (guarded by `typeof window !== 'undefined'`) that calls `push(payload, true)` on `notification-pushed` and `dismiss(payload.id, true)` on `notification-dismissed`. Import `channel` from `../sync/broadcastSync.js` for posting (the mutual import is safe because both references are only accessed inside function bodies, never at module init time — add an inline comment documenting this invariant). Wrap all `channel.postMessage` calls in try/catch; never throw.
  - **Verify**: Import the module in a Node REPL or Vitest test. Call `push({ type: 'overdue', title: 'T', message: 'M' })` six times; confirm the store contains exactly 5 records and the first-pushed record is absent. Call `dismiss` with the id of the second-pushed record; confirm it is gone. Call `clearAll()`; confirm the store is empty. Call `dismiss('nonexistent')`; confirm no error is thrown.
  - **Done**: The store exports `notifications`, `push`, `dismiss`, `clearAll`, and the `Notification` type. Capacity cap is 5. `_fromChannel` flag suppresses broadcast. BroadcastChannel listener is registered once at module init under SSR guard. All calls are no-throw.

- [ ] **Task 1.2: Create due-date checker module**
  - **Files**: `src/lib/notifications/dueDateChecker.ts` (new), ensure `src/lib/notifications/` directory exists
  - **Action**: Create the module. Declare a module-level `const notifiedIds = new Set<string>()`. Export `startDueDateChecker(): () => void`. Inside, define a `runCycle()` helper that reads `get(todos)` from `$lib/stores/todos.js`, computes `today = new Date().toISOString().split('T')[0]`, then for each todo where `!todo.archived && !todo.completed && todo.dueDate !== null && todo.dueDate < today && !notifiedIds.has(todo.id)`, calls `push({ type: 'overdue', title: 'Card Overdue', message: todo.text })` (imported from `$lib/stores/notifications.js`) and adds the id to `notifiedIds`. Call `runCycle()` synchronously, then register `setInterval(runCycle, 60_000)` and return a cleanup that calls `clearInterval`.
  - **Verify**: In an isolated Vitest test with a mocked `todos` store containing one overdue todo and one future-dated todo, call `startDueDateChecker()`. Confirm `push` was called once. Call `startDueDateChecker()` again (same module instance); confirm `push` is not called again for the same id (session dedup). Confirm the future-dated and completed/archived todos produce no calls.
  - **Done**: `startDueDateChecker` runs a synchronous first cycle, schedules 60 s interval, returns a working cleanup. Module-level `notifiedIds` deduplicates across calls within the same session. No throws under empty store.

- [ ] **Task 1.3: Add parseMentions pure function to todos.ts**
  - **Files**: `src/lib/stores/todos.ts` (modify — add export only, no changes to existing logic)
  - **Action**: Add and export the pure function `parseMentions(body: string, collaboratorNames: string[]): string[]` anywhere before `addTodo`. For each name in `collaboratorNames`, escape special regex chars via `name.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')`, then test `/(?<!\w)@(escapedName)(?!\w)/gi` against `body`. If matched, add `name.toLowerCase()` to a result Set. Return `Array.from(resultSet)`. Return `[]` on empty inputs; never throw.
  - **Verify**: Unit-test `parseMentions('@Alice can you check?', ['Alice', 'Bob'])` returns `['alice']`. Test `parseMentions('email@alicewonderland.com', ['Alice'])` returns `[]`. Test `parseMentions('@alice @alice again', ['Alice'])` returns `['alice']` (deduped). Test `parseMentions('@Alice and @Bob', ['Alice', 'Bob'])` returns both. Test name with special char like `'C++'` does not throw.
  - **Done**: `parseMentions` is exported from `todos.ts`. It is pure (no store reads), case-insensitive, whole-word-boundary matching, deduplicating, regex-escaped, and non-throwing. No changes made to any existing function in the file.

## Batch 2 — Modify existing files (parallel; no file overlap between tasks)

- [ ] **Task 2.1: Extend broadcastSync.ts with SyncMessage variants, channel export, and kanban activity diff**
  - **Files**: `src/lib/sync/broadcastSync.ts` (modify)
  - **Action**: (1) Extend the `SyncMessage` union with `{ type: 'notification-pushed'; payload: Notification }` and `{ type: 'notification-dismissed'; payload: { id: string } }` — import `Notification` from `$lib/stores/notifications.js`. (2) Change `let channel` to `export let channel` — or add a `export { channel }` statement at the bottom — so `notifications.ts` can import it. (3) Add the internal `ActivityChange` type and `diffKanbanStates(previous: KanbanState, incoming: KanbanState, todoMap: Map<string, string>): ActivityChange[]` function per the contract: build `previousCardColumn` and `incomingCardColumn` maps, emit `card-moved` on column-id diff, emit `column-added` when incoming column id absent in previous, emit `column-renamed` when id present but title differs. (4) In `listenForRemoteUpdates`, replace the `kanban-updated` case body: snapshot `const previous = get(kanbanState)` before `onKanban(msg.payload)`, then build `todoMap` from `get(todos)`, run `diffKanbanStates`, and call `push` (imported from `$lib/stores/notifications.js`) for each change using the message strings from the contract. Add `case 'notification-pushed': break;` and `case 'notification-dismissed': break;` to the switch (handled by `notifications.ts` own listener) so they do not fall to the default branch. Import `get` from `svelte/store`, `kanbanState` from `$lib/stores/kanban.js`, and `todos` from `$lib/stores/todos.js`. Add `push` import from `$lib/stores/notifications.js`. Add comment on the `push` import: `// NOTE: only accessed inside the kanban-updated handler body, never at module init — circular-import safe`.
  - **Verify**: In a Vitest test, mock `kanbanState` holding state with card `"c1"` in column `"col-a"` and call the `kanban-updated` handler with an updated state moving `"c1"` to `"col-b"`. Confirm `push` is called with `type: 'activity'` and message `"'<cardTitle>' moved to <colBTitle>"`. Confirm a `todos-updated` message does not trigger any `push` call. Confirm `notification-pushed` and `notification-dismissed` messages do not call `push`.
  - **Done**: `SyncMessage` union includes two new variants. `channel` is exported. `diffKanbanStates` detects card moves, new columns, and column renames. Activity `push` is called for each detected change in the `kanban-updated` handler. New SyncMessage variants have explicit no-op cases in the switch. No existing tests broken.

- [ ] **Task 2.2: Wire mention detection into addComment and addReply in todos.ts**
  - **Files**: `src/lib/stores/todos.ts` (modify)
  - **Action**: At the top of the file add imports: `import { self, activeCollaborators } from './collaborators.js'` and `import { push } from './notifications.js'`. Inside `addComment`, after the `broadcastTodos(get(todos))` call, add the mention-detection block: build `allNames` from `[get(self).name, ...get(activeCollaborators).map(c => c.name)]`, call `parseMentions(trimmed, allNames)`, resolve `selfName = get(self).name.toLowerCase()`, and if `matched.includes(selfName)` call `push({ type: 'mention', title: 'You were mentioned', message: \`Mentioned in ${todo?.text ?? 'a card'} by ${commenterName}\` })` — resolve commenter name from `activeCollaborators` by `actorId`, fallback to `get(self).name`. Apply identical post-write block inside `addReply` using the same pattern (look up the parent todo for its `text`). Do not modify `editComment`, `editReply`, or any other function.
  - **Verify**: In a Vitest test, seed the `self` store as `{ id: 'u1', name: 'Alice' }`. Call `addComment(todoId, '@Alice please review', 'bob-id')` with Bob in `activeCollaborators`. Confirm `push` is called once with `type: 'mention'` and message containing both the card title and `'Bob'`. Call `addComment(todoId, '@Bob please', 'u1')`; confirm `push` is not called (Bob is not self). Call `editComment`; confirm `push` is never called. Call `addReply` with `'@alice lgtm'`; confirm `push` is called once.
  - **Done**: `parseMentions` is called after the store write and broadcast in `addComment` and `addReply`. At most one mention notification per call. `editComment` and `editReply` are unchanged. `actorId` resolves to commenter display name with fallback. All existing `todos.ts` tests pass.

## Batch 3 — New UI components (depend on notifications store from Batch 1)

- [ ] **Task 3.1: Create Toast.svelte component**
  - **Files**: `src/lib/components/Toast.svelte` (new)
  - **Action**: Create the component using Svelte 5 `$props()` rune to declare props `{ id, type, title, message }`. Import `fly` from `svelte/transition`. Import `dismiss` from `$lib/stores/notifications.js`. Apply `in:fly={{ x: 64, duration: 250 }}` and `out:fly={{ x: 64, duration: 200 }}` on the root `<div>`. Set the root div to have a left-border accent: `border-red-500` for `overdue`, `border-blue-500` for `mention`, `border-yellow-400` for `activity` — use a computed Tailwind class (or a `class:` directive) driven by `type`. Inside the body, render a `<span class="sr-only">{title}: {message}</span>` for screen reader consumption, then `<p aria-hidden="true">{title}</p>` and `<p aria-hidden="true">{message}</p>`. Render a `<button type="button" aria-label="Dismiss notification" onclick={() => dismiss(id)}>×</button>`. Implement the auto-dismiss timer using the exact state and logic from the `toast-component-props.md` schema: `remaining = $state(5000)`, `startedAt: number`, `timer: ReturnType<typeof setTimeout> | null = null`, `startTimer()`, `pauseTimer()` (clears timer, reduces remaining), `resumeTimer()` (restarts only when timer is null). Bind `onmouseenter={pauseTimer}` and `onmouseleave={resumeTimer}` on the root div. Call `startTimer()` in `onMount`. In `onDestroy` guard: `if (timer !== null) clearTimeout(timer)`.
  - **Verify**: Mount `Toast.svelte` in a Vitest browser test or Svelte component test. Advance fake timers by 5000 ms; confirm `dismiss` was called. Mount, fire `mouseenter` at 2000 ms, advance 5000 ms more; confirm `dismiss` was NOT called during hover. Fire `mouseleave`; advance remaining ~3000 ms; confirm `dismiss` called. Click the close button at 1000 ms; confirm `dismiss` called immediately. Inspect DOM: `aria-label="Dismiss notification"` present; `sr-only` element contains both title and message; overdue type has red border class.
  - **Done**: Slide-in/out `fly` transition on mount and dismiss. Auto-dismiss after 5000 ms. Hover pause/resume with remaining-time tracking. Close button calls `dismiss(id)`. Type-based left-border accent (red/blue/yellow). ARIA: `sr-only` combined text, `aria-hidden` on visible text, `aria-label` on close button. `onDestroy` clears any active timer.

- [ ] **Task 3.2: Create Toaster.svelte container component**
  - **Files**: `src/lib/components/Toaster.svelte` (new)
  - **Action**: Import `notifications` and `dismiss` from `$lib/stores/notifications.js`. Import `Toast` from `./Toast.svelte`. Render a single `<div>` with `role="status"`, `aria-live="polite"`, and inline style `position: fixed; bottom: 1.5rem; right: 1.5rem; display: flex; flex-direction: column-reverse; gap: 0.5rem; z-index: 9999`. Inside it, render a keyed `{#each [...$notifications].reverse() as n (n.id)}<Toast id={n.id} type={n.type} title={n.title} message={n.message} />{/each}`. No props. No other logic.
  - **Verify**: Mount `Toaster.svelte` with an empty store. Push three notifications with distinct `createdAt` values T1 < T2 < T3. Confirm the DOM contains three Toast elements with T3's content appearing above T1's content. Push a fourth notification; confirm all four are present. Check that `role="status"` and `aria-live="polite"` are on the container. Confirm `aria-live="assertive"` is absent from any element. Dismiss one; confirm it disappears.
  - **Done**: Fixed bottom-right container with `z-index: 9999`. ARIA live region with `role="status"` and `aria-live="polite"`. Keyed `{#each}` over reversed notifications array. One `Toast` per entry. No props required.

## Batch 4 — Wire everything into the page (depends on Batches 1, 2, 3)

- [ ] **Task 4.1: Mount Toaster and initialize due-date checker in +page.svelte**
  - **Files**: `src/routes/+page.svelte` (modify)
  - **Action**: (1) Add `import Toaster from '$lib/components/Toaster.svelte'` to the script block alongside the existing component imports. (2) Add `import { startDueDateChecker } from '$lib/notifications/dueDateChecker.js'`. (3) Inside `onMount`, after the `initPresence()` call, add `const stopDueDateChecker = startDueDateChecker()`. (4) In the `onMount` return cleanup function (the anonymous function returned from `onMount`), add `stopDueDateChecker()` alongside the existing `unsubscribe()` and `destroyPresence()` calls. (5) In the template, add `<Toaster />` as a sibling of `<LabelManager>` and `<ShortcutsHelpModal>` at the bottom of the markup (outside the main content `<div>`). No other changes to the existing `listenForRemoteUpdates` call or its callbacks — the diff logic now runs inside `broadcastSync.ts` itself.
  - **Verify**: Load the app in a browser with a todo that has yesterday's `dueDate` and `completed: false`. Confirm a red-accented toast appears within the first render cycle with title `"Card Overdue"`. Wait 60 s; confirm no duplicate toast. Open a second tab and move a card; confirm the first tab shows a yellow-accented `"Board Updated"` toast. Submit a comment mentioning your own name; confirm a blue-accented `"You were mentioned"` toast appears. Dismiss all toasts; reload; confirm the overdue toast reappears (new session). Inspect DOM; confirm exactly one element with `role="status"` and `aria-live="polite"` is present.
  - **Done**: `<Toaster />` is mounted once at the page root. `startDueDateChecker()` is called in `onMount` and its cleanup function is called in the return cleanup. All three notification triggers (overdue, mention, board activity) are exercised end-to-end. Existing functionality (undo/redo, presence, sync, label manager, shortcuts modal, name prompt) is unaffected.

## Batch 5 — Tests (depend on all implementation batches)

- [ ] **Task 5.1: Unit tests for notifications store**
  - **Files**: `src/lib/stores/notifications.test.ts` (new)
  - **Action**: Write Vitest `describe`/`it`/`expect` tests covering every scenario in the `notification_store` and `notification_broadcast` spec requirements. Tests must include: push adds record and returns its id; dismiss removes correct record; dismiss on unknown id is no-op; clearAll empties store; push at capacity (5) evicts the oldest record; push below capacity does not evict; `_fromChannel = true` skips `postMessage`; `_fromChannel = false` (or omitted) calls `postMessage`; incoming `notification-pushed` channel message calls `push(..., true)` (no re-broadcast); incoming `notification-dismissed` channel message calls `dismiss(..., true)` (no re-broadcast); push succeeds when `channel` is null (BroadcastChannel unavailable). Mock `crypto.randomUUID` to return predictable ids. Mock `BroadcastChannel` or stub the channel reference. Each test resets the store via `clearAll()` in `beforeEach`.
  - **Verify**: `npx vitest run src/lib/stores/notifications.test.ts` exits 0. All scenarios from spec sections `notification_store` and `notification_broadcast` have a corresponding passing `it` block.
  - **Done**: Full coverage of push/dismiss/clearAll behaviors, capacity eviction, `_fromChannel` flag semantics, and BroadcastChannel receive paths. No test depends on real browser APIs.

- [ ] **Task 5.2: Unit tests for dueDateChecker**
  - **Files**: `src/lib/notifications/dueDateChecker.test.ts` (new)
  - **Action**: Write Vitest tests covering every scenario from the `overdue_detector` spec requirement. Use `vi.useFakeTimers()` in `beforeEach` and `vi.useRealTimers()` in `afterEach`. Mock the `todos` store to return controlled Todo arrays. Mock `push` from `notifications.ts` to track call counts and arguments. Tests must include: overdue card (dueDate yesterday, completed false, archived false) triggers exactly one `push` on first cycle; already-notified card does not trigger push on second cycle; card due today is not treated as overdue; card due tomorrow is not treated as overdue; completed overdue card is skipped; archived overdue card is skipped; two overdue cards produce two pushes; cleanup function stops subsequent interval ticks from calling `push`; `push` receives `{ type: 'overdue', title: 'Card Overdue', message: todo.text }`.
  - **Verify**: `npx vitest run src/lib/notifications/dueDateChecker.test.ts` exits 0. All scenarios from the `overdue_detector` spec section have a corresponding passing `it` block.
  - **Done**: All overdue detection predicate combinations tested. Session deduplication verified. Interval scheduling and cleanup verified with fake timers. No real I/O or browser APIs.

- [ ] **Task 5.3: Unit tests for parseMentions**
  - **Files**: `src/lib/stores/todos.test.ts` (new, or append to existing if already present)
  - **Action**: Write a `describe('parseMentions', ...)` block with Vitest tests covering all scenarios from the `mention_parser` spec requirement: mention of current user fires (case-insensitive); partial word match (`email@alice`) does not fire; multiple mentions of same name in one body return deduped result; mention of another collaborator returns that name but not self; empty body returns `[]`; empty collaborator names returns `[]`; name with regex special chars (e.g., `'C++'`) does not throw and matches correctly. Separately, test the `addComment` and `addReply` integration using mocked `self`, `activeCollaborators`, and `push`: confirm `push` is called once on self-mention, not called for other-user mention, not called from `editComment` or `editReply`, and that the message includes both card text and commenter name. Each test resets store state in `beforeEach`.
  - **Verify**: `npx vitest run src/lib/stores/todos.test.ts` exits 0 (all existing tests pass plus new tests). All scenarios from `mention_parser` and `comment_mention_integration` spec sections have passing `it` blocks.
  - **Done**: Pure `parseMentions` function is fully tested in isolation. Integration paths through `addComment` and `addReply` are covered. `editComment` and `editReply` explicitly verified to not call `push`.

- [ ] **Task 5.4: Unit tests for broadcastSync extensions**
  - **Files**: `src/lib/sync/broadcastSync.test.ts` (new, or append to existing if already present)
  - **Action**: Write Vitest tests for the `board_activity_notifications` spec scenarios. Mock `kanbanState` store, `todos` store, and `push` from `notifications.ts`. Tests must include: card moved from one column to another triggers `push` with `"'<title>' moved to <colTitle>"`; card move in current tab does not fire (BroadcastChannel never delivers to sender — test by asserting `push` is not called when `broadcastKanban` is called locally, i.e., the `listenForRemoteUpdates` handler is only triggered by simulating a received message); new column added triggers `push` with `"New column '<title>' added"`; column renamed triggers `push` with `"Column renamed to '<title>'"` ; multiple simultaneous changes (one new column + one rename) produce exactly two `push` calls; `todos-updated` message does not call `push`; `notification-pushed` and `notification-dismissed` messages do not call `push`. Also test that `SyncMessage` type includes the two new variants (TypeScript compilation check — can be a type-level assertion).
  - **Verify**: `npx vitest run src/lib/sync/broadcastSync.test.ts` exits 0. All scenarios from `board_activity_notifications` spec section have passing `it` blocks.
  - **Done**: `diffKanbanStates` logic is thoroughly tested through the `kanban-updated` message handler. All three change kinds (card-moved, column-added, column-renamed) produce correct message strings. Non-kanban message types do not produce activity toasts.
